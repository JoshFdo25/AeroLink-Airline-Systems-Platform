apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: aerolink
  labels:
    app: booking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: booking-service
          image: 643942183295.dkr.ecr.us-east-1.amazonaws.com/aerolink-booking-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: aerolink-config
          livenessProbe:
            httpGet:
              path: /api
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: aerolink
spec:
  selector:
    app: booking-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3003
