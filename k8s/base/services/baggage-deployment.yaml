apiVersion: apps/v1
kind: Deployment
metadata:
  name: baggage-service
  namespace: aerolink
  labels:
    app: baggage-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: baggage-service
  template:
    metadata:
      labels:
        app: baggage-service
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: baggage-service
          image: 643942183295.dkr.ecr.us-east-1.amazonaws.com/aerolink-baggage-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: aerolink-config
          livenessProbe:
            httpGet:
              path: /api
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: baggage-service
  namespace: aerolink
spec:
  selector:
    app: baggage-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3004
